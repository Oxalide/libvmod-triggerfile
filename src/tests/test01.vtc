varnishtest "Test triggerfile vmod"

server s1 {
       rxreq
       txresp
} -start

varnish v1 -vcl+backend {
	import triggerfile from "${vmod_topbuild}/src/.libs/libvmod_triggerfile.so";

	sub vcl_deliver {
		set resp.http.hello = triggerfile.hello("World");
	}
} -start

client c1 {
	txreq -url "/"
	rxresp
	expect resp.http.hello == "Hello, World"
}

client c1 -run
